<%= form_with(model: album_image) do |form| %>
  <% if album_image.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(album_image.errors.count, "error") %> prohibited this album_image from being saved:</h2>

      <ul>
        <% album_image.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :album_id, style: "display: block" %>
    <%= form.text_field :album_id %>
  </div>

  <div>
    <%= form.label :image_id, style: "display: block" %>
    <%= form.text_field :image_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
